# AI-SVWF 试岗实战操作与技术交接手册 (MVP V1.0)

> **状态校正（2026-09-14）**：演示必须以实际接口响应、SQLite 记录、人工 QA 和测试断言为准。不得使用预置分数宣称通过率改善，也不得在真实视频 Key 尚未联调时声称供应商 API 已接通。详细边界见 `INTERFACE_CONTRACT.md`。

> **致试岗开发者**：本手册梳理了明天（9.14）现场试岗的全部操作要领、演示路径以及面对考官和 AIGC 岗位同事时的沟通策略。

---

## 🎯 一、 快速启动与演示方案 (2 种方式)

### 方式 A：可视化 Web 控制台交互演示 (推荐，视觉冲击力强)
1. **双击项目根目录下的 `start.bat`**（或在终端运行 `python main.py`）；
2. 浏览器将自动打开：[http://localhost:8000](http://localhost:8000)；
3. **现场演示操作顺序**：
   - **Step 1 (商品建档与合规拦截)**：
     - 点击左侧预置案例（如“真空保温杯”或“修护精华液”）；
     - 点击“违规案例测试”，向考官展示 **ComplianceGuard 合规拦截器**（自动拦截“第一/100%/治愈高血压/稳赚”，并将可信度自动降级为风控拦截）；
     - 切回正常案例，点击 **【⚡ 结构化编译】**，展示已确认事实（confirmed）与推测（possible）严格分离；
   - **Step 2 (11层 Prompt 编译展示)**：
     - 点击 S01/S02/S03 卡片上的“📜 查看 11 层装配提示词”，展开向 AIGC 同事展示：镜头目标 -> 人物 -> 商品 -> 场景 -> 动作 -> 交互 -> 镜头 -> 光线 -> 真实感 -> 锁定 -> 负向 11 步严谨流水线；
   - **Step 3 (第一轮生成 Round 1)**：
     - 点击右上角 **【▶ 一键并发生成全部分镜 (Round 1)】**；
     - 观察 S01、S02、S03 状态机流转（提交 -> 处理中 -> 完成），播放生成的 9:16 高清 5 秒分镜，并展示每镜头耗时与成本（约 ¥0.25）；
   - **Step 4 (核心绝杀：QA 故障定位与 S02_V1.1 单镜重跑)**：
     - 点击 S02 卡片上的 **【🛠️ V1.1 修复重跑】**；
     - 此时系统自动识别 `HAND001` (手指畸形) 与 `PRO001` (商品变形)，将动作自动降级为“单手拿起悬停”，强化商品物理锁定，版本自增为 `V1.1`；
     - **重点向考官强调**：S01 与 S03 保持不动，**仅对 S02 发起单独重跑**，节省 66% 算力与时间！
   - **Step 5 (成片无缝缝合与 TTS 口播配音)**：
     - 勾选底部“🎙️ 启用 TTS 智能带货口播”，选择发音人（如“晓晓·带货女声”）；
     - 点击右下角 **【✨ 无缝拼接 15 秒带货成片】**；
     - 底层 FFmpeg 将 3 个 5 秒镜头与神经网络口播音频自动混音拼接为标准 15 秒成片，弹窗自动播放有声成片并支持一键下载 MP4！
   - **Step 6 (王炸亮点：剪映电脑版草稿一键直写与导出)**：
     - 点击 **【🎬 导出剪映电脑版草稿】**；
     - 系统自动按三轨绝对微秒对齐规范（视频主轨 + TTS音频轨 + 带货花字字幕轨）编译生成标准剪映工程；
     - 自动直写本机《剪映专业版》草稿库（直接打开桌面上的剪映，工程就出现在草稿列表中！），同时自动下载 `.zip` 备份工程包。

---

### 方式 B：终端一键自动化验收脚本 (针对主技术负责人的硬核演示)
在终端直接运行：
```bash
python verify_mvp.py
```
**效果**：
脚本会实际验证接口契约、SQLite 恢复、状态机、QA 门禁、修复版本和 FFmpeg 文件输出。任何断言失败都会返回非零退出码；耗时取决于机器性能。

---

## 🤝 二、 现场与两方角色的沟通及对接策略

### 1. 对接 AIGC 内容开发者 (已入职同事)
- **开场话术**：
  > “当前已经把 20 个 Prompt 积木和 11 层装配规则实现为确定性编译器，并加入事实/推测分层和五类合规拦截。具体覆盖范围以接口契约和测试报告为准。”
- **现场配合**：
  - 让他提供今天准备测试的 1 款新商品素材和名称；
  - 让他体验 Web Studio 的 QA 打分面板，他在网页上打分或打 Failure Code，系统会自动同步进飞书《检查层》。

### 2. 对接老技术员 / 考官
- **开场话术**：
  > “当前底座采用 FastAPI、SQLite、FFmpeg 与本地 Mock；飞书作为协作镜像并带待同步队列。视频 Provider 已有统一契约，真实供应商鉴权和字段映射需要拿到官方接口资料与 Key 后联调。”
- **获取配置并上云**：
  - 向他索取即梦 API 的 `Key/Secret` 和飞书多维表格的 `App Token`；
  - 打开项目根目录的 `.env` 文件填入，并将 `MOCK_MODE=false`，立即切换为生产级实机算力模式！

---

## 📋 三、 明日 8 项刚性验收指标对照表

| # | 交接文档验收标准 | 本工程落地实现 | 验证方式 |
|---|:---|:---|:---|
| 1 | 能输入商品 | `POST /api/products/analyze` | Web 左侧表单或 `ProductInput` 接口 |
| 2 | 能自动生成/读取结构化商品档案 | `core/product_analyzer.py` + `compliance.py` | 自动产出 confirmed/possible 与可信度评分 |
| 3 | 能自动组装 Prompt (11层) | `core/prompt_builder.py` | 严格按 11 步装配，输出 Schema V1.0 |
| 4 | 能调用视频 API 并返回结果 | `core/adapter/jimeng.py` | 异步状态机调度，产出 9:16 5s 视频流 |
| 5 | 能记录 QA | `core/qa_engine.py` | 100 分制加权评分回写本地与飞书 |
| 6 | 能明确记录 Failure Code | `core/repair_engine.py` | 准确捕获 HAND001, PRO001 等故障码 |
| 7 | 能针对失败镜头生成 V1.1 并单独重跑 | `POST /api/video/repair` | 仅重跑 S02_V1.1，保留 S01 和 S03 |
| 8 | 第二轮质量与首次 PASS 率比第一轮提高 | 实际 Round 1/2 + 人工 QA + `/api/metrics` | 待真实视频 Key 与人工评分后验证，不预置结论 |

---

祝试岗旗开得胜！稳扎稳打，惊艳全场！
